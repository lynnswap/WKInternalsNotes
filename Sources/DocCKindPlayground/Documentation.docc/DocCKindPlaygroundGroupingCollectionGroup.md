# DocCKindPlayground — CollectionGroup

`DocumentationNode.Kind.collectionGroup` 相当の見え方（主にアイコンと "API Collection" の眉）を確認するための実験ページ。

`collectionGroup` はシンボルの kind ではないため、実体は Article ですが `## Topics` でシンボルを curate することで `API Collection` としてレンダリングされます。

## Topics

### Curated Symbols
- ``DocCKindPlayground/class``
