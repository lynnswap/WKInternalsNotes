# ``WKInternalsNotes/WKGlobals``

トップレベルの `@protocol` / `typedef` / `NS_ENUM(NS_OPTIONS)` / `extern` をまとめる擬似コンテナ。

## Discussion
DocC のナビゲーションでモジュール直下が雑多になりすぎないように、C/Objective-C のトップレベル宣言を `WKGlobals` 配下に集約する。

## Metadata
| Key | Value |
| --- | ----- |
| Status | Draft |
| Last updated | 2025-12-18 |

